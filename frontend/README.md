# Agent Reputation Passport Frontend

Minimal Vite React frontend for testing MetaMask wallet ownership verification.

## Setup

```powershell
npm install
npm run dev
```

Backend must be running:

```powershell
uvicorn app.main:app --reload
```

Open:

```text
http://localhost:5173
```

MetaMask extension must be installed in the browser.
